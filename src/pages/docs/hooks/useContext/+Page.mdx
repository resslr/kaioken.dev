import { siteCodeLang } from "$/state/siteCodeLang"
import ExampleCode from "./example.md"
import ExampleCodeTS from "./example.ts.md"

# useContext

Gets the value from a <a href="/docs/api/context">Context</a> Provider component within the current component's heirarchy. If no provider is found, it will return the context's default value.

### Example

{siteCodeLang.value === "ts" ? <ExampleCodeTS /> : <ExampleCode />}

### Related
<ul>
  <li><a href="/docs/api/context">Context</a></li>
</ul>