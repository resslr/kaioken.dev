import type { CodePreviewData } from "$/types"
import WatchEffectType from "./WatchEffect.type.md"

export const watchEffectTypePreview = {
  element: WatchEffectType,
  link: {
    text: "WatchEffect",
    href: "/docs/api/signal#watch",
  },
} satisfies CodePreviewData
