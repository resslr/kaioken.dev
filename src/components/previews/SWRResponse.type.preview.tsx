import type { CodePreviewData } from "$/types"
import SWRResponseType from "./SWRResponse.type.md"

export const swrResponseTypePreview = {
  element: SWRResponseType,
  link: {
    text: "SWRResponse",
    href: "/docs/api/swr",
  },
} satisfies CodePreviewData
