import type { CodePreviewData } from "$/types"
import UseAsyncResultType from "./UseAsyncResult.type.md"

export const UseAsyncResultTypePreview = {
  element: UseAsyncResultType,
  link: {
    text: "UseAsyncResult",
    href: "/docs/hooks/useAsync",
  },
} satisfies CodePreviewData
