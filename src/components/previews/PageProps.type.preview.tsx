import type { CodePreviewData } from "$/types"
import PagePropsType from "./PageProps.type.md"

export const pagePropsTypePreview = {
  element: PagePropsType,
  link: {
    text: "PageProps",
    href: "/docs/api/file-router#pageprops",
  },
} satisfies CodePreviewData
