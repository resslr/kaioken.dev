import { ContextExample } from "./ContextExample"
import { CodePreview } from "$/components/CodePreview"
import { createContextPreview } from "$/components/previews/createContext.preview"
import { contextPreview } from "$/components/previews/context.preview"

# Context

<CodePreview data={contextPreview} /> is an external 
state object declared via <CodePreview data={createContextPreview} />.
The <b>Provider</b> property is used as a component that acts as a 'reactive boundary'.
When the context's state changes, children of the provider will be updated.

### Example

<div className="not-prose">
  <ContextExample />
</div>

### Related
<ul>
  <li><a href="/docs/hooks/useContext">useContext</a></li>
</ul>