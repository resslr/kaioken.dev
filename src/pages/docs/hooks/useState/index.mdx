import { Head, Link } from "kiru/router"
import { CopyInnerText } from "$/components/CopyInnerText"

<Head.Content>
  <title>Kiru - useState</title>
  <meta name="keywords" content="useState, hooks" />
  <meta name="description" content="Learn how to use useState hook in Kiru to create reactive state variables within components that trigger re-renders when updated." />
</Head.Content>

# useState

Allows you to create a reactive state variable within a component.
Changes will trigger re-renders of the component to reflect the 
updated state.


### Example

<CopyInnerText>
```jsx
import { useState } from "kiru"

function App() {
  const [count, setCount] = useState(0) // Initial state is 0

  const handleClick = () => {
    setCount((prev) => prev + 1) // Update state using setCount
  }

  return (
    <div>
      <p>You clicked {count} times</p>
      <button onclick={handleClick}>Click me</button>
    </div>
  )
}
```
</CopyInnerText>

### Related
<ul>
  <li><Link to="/docs/hooks/useReducer">useReducer</Link></li>
</ul>