import {ExternalLink} from '$/components/atoms/ExternalLink'
import {TerminalCodeBlock} from '$/components/TerminalCodeBlock'
import {JSONCodeBlock} from '$/components/JSONCodeBlock'
import {CodeBlock} from '$/components/CodeBlock'
import {GitCloneCode} from './GitCloneCode'

# Getting started

<h2 id="start-a-new-project">Start a new project</h2>

To quickly scaffold a new project, clone the <ExternalLink href="https://github.com/CrimsonChi/kaioken-csr-template.git">CSR Template</ExternalLink>
or the <ExternalLink href="https://github.com/CrimsonChi/kaioken-ssr-template.git">SSR/SSG Template</ExternalLink>

<div className="not-prose">
  <GitCloneCode />
</div>


<h2 id="add-to-an-existing-project">Add to an existing project</h2>

The following example demonstrates adding Kaioken to a Vite project.

<TerminalCodeBlock 
  code={`pnpm add kaioken
pnpm add vite-plugin-kaioken -D`}
  />

<div className="no-prose">
  <span>Then, update your vite config to add JSX settings and the dev plugin:</span>
  <CodeBlock code={`import { defineConfig } from "vite"
import kaioken from "vite-plugin-kaioken"

export default defineConfig({
  esbuild: {
    jsxInject: \`import * as kaioken from "kaioken"\`,
    jsx: "transform",
    jsxFactory: "kaioken.createElement",
    jsxFragment: "kaioken.fragment",
    loader: "tsx",
    include: ["**/*.tsx", "**/*.ts", "**/*.jsx", "**/*.js"],
  },
  plugins: [kaioken()],
})`} className='my-0' />
</div>
<br />

<div className="no-prose">
  <span>Lastly, add the following entry in your tsconfig.json:</span>
  <JSONCodeBlock code={`{
  "compilerOptions": {
    ...
    "jsx": "preserve"
    ...
  }
}`} className='my-0' />
</div>