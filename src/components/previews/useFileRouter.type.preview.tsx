import type { CodePreviewData } from "$/types"
import UseFileRouterType from "./useFileRouter.type.md"

export const useFileRouterTypePreview = {
  element: UseFileRouterType,
  link: {
    text: "useFileRouter",
    href: "/docs/api/file-router#usefilerouter",
  },
} satisfies CodePreviewData
