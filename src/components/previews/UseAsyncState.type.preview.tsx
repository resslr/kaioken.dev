import type { CodePreviewData } from "$/types"
import UseAsyncStateType from "./UseAsyncState.type.md"

export const UseAsyncStateTypePreview = {
  element: UseAsyncStateType,
  link: {
    text: "UseAsyncState",
    href: "/docs/hooks/useAsync",
  },
} satisfies CodePreviewData
