# Hooks

Kaioken implements many familiar hooks you might have seen in React,
which maintain the same functionality and logic. It also implements
utility hooks and the ability to
create <a className="text-nowrap" href="/docs/api/custom-hooks">custom hooks</a>.
