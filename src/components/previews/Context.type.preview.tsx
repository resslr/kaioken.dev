import type { CodePreviewData } from "$/types"
import ContextType from "./Context.type.md"

export const contextTypePreview = {
  element: ContextType,
  link: {
    text: "Context",
    href: "/docs/api/context",
  },
} satisfies CodePreviewData
