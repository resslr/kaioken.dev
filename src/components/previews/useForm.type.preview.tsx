import type { CodePreviewData } from "$/types"
import UseFormType from "./useForm.type.md"

export const useFormTypePreview = {
  element: UseFormType,
  link: {
    text: "useForm",
    href: "/docs/api/form",
  },
} satisfies CodePreviewData
