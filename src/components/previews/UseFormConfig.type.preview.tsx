import type { CodePreviewData } from "$/types"
import UseFormConfigType from "./UseFormConfig.type.md"

export const useFormConfigTypePreview = {
  element: UseFormConfigType,
  link: {
    text: "UseFormConfig",
    href: "/docs/api/form",
  },
} satisfies CodePreviewData
