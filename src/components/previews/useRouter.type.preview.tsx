import type { CodePreviewData } from "$/types"
import UseRouterType from "./useRouter.type.md"

export const useRouterTypePreview = {
  element: UseRouterType,
  link: {
    text: "useRouter",
    href: "/docs/api/routing",
  },
} satisfies CodePreviewData
