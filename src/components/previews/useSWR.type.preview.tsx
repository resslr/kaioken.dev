import type { CodePreviewData } from "$/types"
import UseSWRType from "./useSWR.type.md"

export const useSWRTypePreview = {
  element: UseSWRType,
  link: {
    text: "useSWR",
    href: "/docs/api/swr",
  },
} satisfies CodePreviewData
