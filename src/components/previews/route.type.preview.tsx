import type { CodePreviewData } from "$/types"
import RouteType from "./route.type.md"

export const routeTypePreview = {
  element: RouteType,
  link: {
    text: "Route",
    href: "/docs/api/router#route",
  },
} satisfies CodePreviewData
