import type { CodePreviewData } from "$/types"
import FileRouterType from "./FileRouter.type.md"

export const fileRouterTypePreview = {
  element: FileRouterType,
  link: {
    text: "FileRouter",
    href: "/docs/api/file-router#filerouter",
  },
} satisfies CodePreviewData
