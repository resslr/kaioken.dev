import type { CodePreviewData } from "$/types"
import UseHookCallbackType from "./useHookCallback.type.md"

export const useHookCallbackTypePreview = {
  element: UseHookCallbackType,
  link: {
    text: "Callback",
    href: "/docs/hooks/custom-hooks",
  },
} satisfies CodePreviewData
